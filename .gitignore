# Config (contains passwords & API keys - NEVER commit!)
config.php
config.php.*
deploy-secret.txt
*.LOCAL_ONLY*

# Archives / accidental server dumps
*.zip
github.zip
smm_panel*.zip
SMM_Panel*.zip

# Nested duplicate trees (do not ship)
cloudflaremanagers.com/

# Android app build output
android-app/branding-tmp/
android-app/local.properties
android-app/.gradle/
android-app/build/
android-app/app/build/
android-app/.idea/
android-app/*.iml

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temp / Cache
tmp/
temp/
cache/
*.cache

# Backup
*.bak
*.backup

# Runtime child-panel data (keep structure if needed later)
storage/child-panels/*/
!storage/child-panels/.gitkeep

# User uploads (keep uploads/.htaccess via negation)
uploads/*
!uploads/.htaccess

# Security/local files (Codex senior audit)
.env
.env.*
!.env.example
!.env.sample
*.env.bak
*.env.backup
*secret*.json
*service-account*.json
firebase-service-account.json
google-services.json
*.pem
*.key
*.p8
*.p12
*.jks
*.keystore
secrets/
.secrets/
